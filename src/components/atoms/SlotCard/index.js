import React from 'react';
import {View, Text} from 'react-native';
import {Card} from '_styles/SlotCard';

const SlotCard = (props) => {
  return (
    <Card>
      <Text></Text>
    </Card>
  );
};

export default SlotCard;
